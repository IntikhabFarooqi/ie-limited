export const metadata = {
  title: "Broadcasting Services | Glonect",
  description: "Get scalable, secure, and modern web development tailored to your business needs. Glonect builds custom websites for growth.",
};

export default function Layout({ children }) {
  return children;
} 